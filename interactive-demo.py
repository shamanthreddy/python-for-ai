# Lets explore interactive mode
name = "Python Learner"
print(name)
print(f"Hello, {name}");

# Some sata to work with
numbers = [1, 2, 3, 4, 5]
print(f"Nunbers: {numbers}")

# Calculate something
total = sum(numbers)
print(f"Total: {total}")

# First create a variable
message = "Hello"

# Later, use it
print(message + "World!")

# Modify it
message = message.upper()
print(message)